// This file is kept as a placeholder in case we need Firebase client-side functionality in the future.
// Currently, all Firebase operations are handled by the backend server.

console.log("Firebase client-side integration is disabled - using backend API instead");

// Mock exports to not break imports in other files
export const auth = {};
export const db = {};

export default {}; 